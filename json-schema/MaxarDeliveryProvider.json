{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://api.sentinel-hub.com/schemas/MaxarDeliveryProvider",
  "title": "MaxarDeliveryProvider",
  "type": "object",
  "properties": {
    "provider": {
      "description": "Provider of the delivery",
      "type": "string",
      "enum": [
        "MAXAR"
      ]
    }
  }
}